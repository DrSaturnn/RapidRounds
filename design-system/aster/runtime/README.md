# Aster Runtime Exports

Runtime exports are approved app-facing assets derived from the canonical Aster
source and future rigged model.

The current runtime source is:

```text
neutral.png
```

The browser-facing copy is:

```text
public/aster/runtime/neutral.png
```

Feature code should import Aster through `components/aster/AsterAssets.ts`, not
by hardcoding asset paths.
